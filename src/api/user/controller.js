const userService = require('./service');

module.exports = {
    /*
    async getTree(req, res) {
        try {
            const tree = await citizenService.getTree();
            res.status(200).send({
                data: tree
            });
        } catch (e) {
            res.error(e);
        }
    },*/
};
